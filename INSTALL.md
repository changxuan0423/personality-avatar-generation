# 安装

本仓库含两个独立 skill 目录：

- `everme-digital-avatar-generation/`
- `screenprint-character-illustration/`

将需要的目录复制到 Codex skills 目录：

```bash
cp -R <skill-directory> ~/.codex/skills/
```

完成后重新开启一个 Codex 会话。

`everme-digital-avatar-generation/` 同时保留原有的 `install.sh`；仓库根目录的压缩包为该 skill 的兼容安装包。
