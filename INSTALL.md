# 安装

本仓库含两个独立 skill 目录：

- `american-comic-halftone-avatar/`
- `screenprint-character-illustration/`

将需要的目录复制到 Codex skills 目录：

```bash
cp -R <skill-directory> ~/.codex/skills/
```

完成后重新开启一个 Codex 会话。

`american-comic-halftone-avatar/` 同时提供 `install.sh`；仓库根目录的同名压缩包是便携安装包。
